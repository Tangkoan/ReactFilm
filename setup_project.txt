52. The Beginning
    > npx create-react-app ./

    - File public/index.html សម្រាប់ Pageដែលយើងចូលដំបូង
53. Create Git

54. Dependency
    > npm install react-router-dom
    > npx audit fix (បើអានេះមានបញ្ហាប្រើCommand ខាងក្រោមបន្ទាប់)
    > npm audit fix --force
    > npm install @alan-ai/alan-sdk-web @mui/icons-material @mui/material @mui/styles @reduxjs/toolkit axios react-redux --legacy-peer-deps
    > npm install react-scripts@5.0.1(ករណី 0.0.0 នៅត្រង់ package.json វាមាន react-scripts@0.0.0)

    -- File package.json ប្ដូរទៅពីកូដខាងលើទៅខាងក្រោម
        "scripts": {
            "start": "react-scripts start",
            "build": "react-scripts build",
            "test": "react-scripts test",
            "eject": "react-scripts eject"
        },

        => 

        "scripts": {
            "start": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
            "build": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
            "test": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts test",
            "eject": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts eject"
        },



55. Intro & Documentation
    
        🕵🏽 Code : 
                -- រៀបចំ File App.jsx ដែលមាន Ipmort របស់មួយចំនួនមកប្រើដើម្បីងាយស្រួលសរសេរកូដ
                    import {CssBaseline} from "@mui/material";
                    import { BrowserRouter, Routes, Route } from "react-router-dom";
                    import Movies from "./Movies/Movies";
                    import Actors from "./Actors/Actors";
                    import NotFound from "./NotFound/NotFound";
                    const App = () => {
                        return (
                        <div>
                            <CssBaseline />
                                <BrowserRouter>
                                    <Routes>
                                        <Route path="/" element={<Movies/>}/>
                                        <Route path="/actors/:id" element={<Actors/>}/>
                                        <Route path="*" element={<NotFound/>}/>
                                    </Routes>
                                </BrowserRouter>
                        </div>
                        );
                    }
                    export default App;

                -- រៀបចំ File NotFound.jsx
                    import React from 'react'
                    const NotFound = () => {
                    return (
                        <div>
                        <h1>Not Found</h1>
                        </div>
                    )
                    }
                    export default NotFound;

                -- រៀបចំ File Actors.jsx
                        import React from 'react'
                        const Actors = () => {
                        return <div>Actors Page</div>;
                        };
                        export default Actors;

                -- រៀបចំ File Movies.jsx
                    import { Movie } from "@mui/icons-material"
                    const Movies = () => {
                    return(
                        <div>
                            <h1>Movies Page</h1>
                        </div>
                    )
                    }
                    export default Movies;



    > npm install @emotion/react @emotion/styled --legacy-peer-deps ធ្វើការ Run Command នេះមុននឹងចុចលើBrowsser ដើម្បីមើលលទ្ធផល





56. Component Structure & Routing
        🕵🏽 Code :
                import {CssBaseline} from "@mui/material";
                import { BrowserRouter, Routes, Route } from "react-router-dom";
                import Movies from "./Movies/Movies";
                import Actors from "./Actors/Actors";
                import NotFound from "./NotFound/NotFound";
                import NavBar from "./NavBar/NavBar";
                import { Movie } from "@mui/icons-material";
                import Profile from "./Profile/Profile";

                const App = () => {
                    return (
                    <div>
                        <CssBaseline />
                        <BrowserRouter>
                        <NavBar />
                        <main>
                            <div>
                            <Routes>
                                <Route path="/movie/:id" element={<Movie />} />
                                <Route path="/" element={<Movies />} />
                                <Route path="/actors/:id" element={<Actors />} />
                                <Route path="*" element={<NotFound />} />
                                <Route path="/profile/:id" element={<Profile/>}/>
                            </Routes>
                            </div>
                        </main>
                        </BrowserRouter>
                    </div>
                    );
                }

                export default App;
